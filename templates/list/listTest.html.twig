{% if list %}
<!--
        <table id="list" class="table table-striped">
            <thead>
            <tr>
                <th>Position</th>
                <th>Level</th>
                <th>Details</th>
                <th>Creator</th>
            </tr>
            </thead>
            <tbody>
            {% for list in list %}
                <tr>
                    <td>{{ list.place }}</td>
                    <td>{{ list.name }}</td>
                    <td>{{ list.creator }}</td>
                    <td><a href="/list" class="btn btn-info">More</a></td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    {% else %}
        <p>Error: No levels currently placed on the list</p>
    -->
